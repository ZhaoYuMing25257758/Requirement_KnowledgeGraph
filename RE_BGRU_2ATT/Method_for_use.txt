（1）首先导入整个项目，在train_GRU.py中，可以对相应的数据集进行训练，同时训练生成的模型存放在model文件夹中，文件夹中存放的是我的我自己的训练的结果，所以，需要进训练自己的设计的数据时，可以根据先删除，然后在开始训练。
（2）训练结束以后（因为是神经网络，所以训练的时间相对较长，所以需要耐心等待），可以直接打开对应的test_GRU.py.然后，测试训练的模型。
（3）整个项目的词向量，都是不需要读者自己的训练的，直接在data的vec.txt文本存放。里边是机器直接读取的文本，所以，尽管打开我们也无法读懂文本的意思。